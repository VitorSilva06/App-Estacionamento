rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Libera leitura e escrita para usuários logados
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
